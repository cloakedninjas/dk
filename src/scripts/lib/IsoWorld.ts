///<reference path="../../reference.d.ts"/>

module DK {
    export class IsoWorld extends Phaser.Group {
        add (child: any, silent?: boolean) {
            super.add(child, silent);
        }
    }
}